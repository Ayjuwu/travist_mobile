<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp">

    <TextView
        android:id="@+id/tvAlteredState"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Visite altérée"
        android:textColor="#FFA000"
        android:textStyle="bold"
        android:visibility="gone"
        android:layout_marginTop="4dp"/>

    <LinearLayout
        android:id="@+id/alteredButtonsContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:visibility="gone"
        android:layout_marginTop="8dp">

        <Button
            android:id="@+id/btnDeleteKp"
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:backgroundTint="#323364"
            android:fontFamily="sans-serif-condensed"
            android:textColor="#dddefa"
            android:text="Supprimer du voyage"/>

        <Space
            android:layout_width="16dp"
            android:layout_height="wrap_content"/>

        <Button
            android:id="@+id/btnModifyKp"
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:backgroundTint="#323364"
            android:fontFamily="sans-serif-condensed"
            android:textColor="#dddefa"
            android:text="Remplacer ce lieu"/>
    </LinearLayout>
</LinearLayout>